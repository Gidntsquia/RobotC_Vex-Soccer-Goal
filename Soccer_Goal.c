#pragma config(Sensor, dgtl1,  SonarRight,     sensorSONAR_cm)
#pragma config(Sensor, dgtl3,  SonarLeft,      sensorSONAR_cm)
#pragma config(Sensor, dgtl6,  TouchLeft,      sensorTouch)
#pragma config(Sensor, dgtl7,  TouchRight,     sensorTouch)
#pragma config(Sensor, dgtl11, red,            sensorLEDtoVCC)
#pragma config(Sensor, dgtl12, green,          sensorLEDtoVCC)
//*!!Code automatically generated by 'ROBOTC' configuration wizard               !!*//

task main() {
    while (1 == 1) {
        if (SensorValue(SonarRight) < 14) {
            turnLEDOn(red);
        }
        if (SensorValue(SonarLeft) < 14) {
            turnLEDOn(green);
        }
        if (SensorValue(SonarRight) >= 14) {
            turnLEDOff(red);
        }
        if (SensorValue(SonarLeft) >= 14) {
            turnLEDOff(green);
        }
    }
}
